# RT-ResearchFlow v<version>

> **本文件是发版说明模板，不参与发版。** 每次发版时从本模板复制为 `docs/releases/v<version>.md`（例如 `docs/releases/v0.1.0-beta.6.md`），把全部 `<version>` 占位符与 `（…）` 填写指引替换为真实内容后再提交。`.github/workflows/release.yml` 会强制校验 `docs/releases/v<package.json version>.md` 存在，缺失则发版工作流直接失败。
>
> 写作要求：只描述本版本真实具备的能力；版本号、文件名与 `package.json`、Release 产物严格一致；不引入根目录 CHANGELOG。骨架提炼自 v0.1.0-beta.1 至 beta.5 五份发版说明，「本版本更新」小节结构参照 beta.4（上游合入 / 本 fork 独有）与 beta.5（含工程质量）的公共要素，归并为三段式。

这是 RT-ResearchFlow 的（第 N 个公开测试版本 / 首个公开测试版本）（本 fork）。相对（上一个已发布版本），本版（一句话概括本版主线：合入了哪些上游能力、新增了哪些本 fork 独有能力、修复了什么）。

> 本项目不是荐股机器人，不承诺收益，不提供自动交易。所有结论都应由用户结合原始证据独立判断。

## 下载与校验

- Windows 10/11 x64：`RT-ResearchFlow-Setup-<version>-x64.exe`
- 完整性校验：下载同一 Release 中的 `SHA256SUMS.txt`，在 PowerShell 执行：

```powershell
Get-FileHash .\RT-ResearchFlow-Setup-<version>-x64.exe -Algorithm SHA256
```

输出应与 `SHA256SUMS.txt` 中的哈希一致。（若尚未代码签名，保留以下表述：当前测试版尚未提供代码签名，Windows 可能显示 SmartScreen 提示；请只从本仓库 Releases 页面下载安装包。若已完成商业签名，如实改写为签名状态描述。）

## 本版本更新

### 上游合入

（此处列出从上游 `caoritian002-wq/RT-ResearchFlow` 合入或对照采纳的能力，逐条写清用户可感知的行为变化与对应 Migration/FR 编号；必须与代码真实能力一致。若本版无上游合入，写「本版无上游合入」。）

### 本 fork 独有

（此处列出相对上游的本 fork 独有改进，例如 Agent Context Engine、Cursor 式会话面、趋势 AI 锚定偏差分、自定义数据目录等；每条注明解决的问题或对应归档。必须与代码真实能力一致。）

### 工程质量

（此处列出发版前 review、`verify` 门禁暴露并修复的类型错误、单测失败、冗余清理、Migration 废弃等工程性改动；写明数量与范围。若无，可省略本小节。）

## 安装、升级与卸载

- 安装器支持当前用户安装和自选安装目录，并创建桌面与开始菜单快捷方式。
- 应用数据保存在安装目录下的 `data` 目录。由（可升级的旧版本范围，如 beta.1–beta.5）升级到本版会保留该目录（如涉及数据库结构变化，注明自动向前 Migration）。
- 卸载时会询问是否同时删除本地数据，默认选择为保留。（如建议备份，注明「升级或卸载前建议备份整个 `data` 目录」。）

## AI 与数据源

- 首次启动不要求 AI Key 或 Tushare Token，可以先使用公共数据源完成基础数据准备。
- AI 分析、深度研究和多视角复核需要用户自行配置兼容的模型服务。
- Tushare 仍是可选增强数据源；部分财务、筹码、题材和策略数据是否可用取决于账号权限。
- 公共历史日线主要用于补齐本地研究基座，不承担实时交易行情职责。
- （若本版改变上述任何边界，如实增删；边界未变则保持以上表述。）

## 已知限制

- （此处逐条列出当前版本的真实限制：平台覆盖、代码签名、尚未启用的服务、已知技术债等，必须与代码真实能力一致。）
- （沿用项示例：本次只提供 Windows x64 安装包，macOS 尚未纳入发布验收。）
- （沿用项示例：安装包尚未进行商业代码签名。）
- （固定项，每版必须保留：应用不执行下单、自动交易、仓位控制或收益预测。）

## 反馈

- 可复现 Bug：提交 Issue，并附版本、复现步骤、预期与实际结果。日志和截图须先移除 Token、持仓及个人路径等敏感信息。
- 安装与使用问题、想法和工作流交流：使用 Discussions。
- 安全问题：使用仓库 Security 页面中的私密漏洞报告，不要创建公开 Issue。
