# 指挥区四指标卡均可点击跳转 — 设计

**状态：** 已完成  
**日期：** 2026-08-11  
**用户确认：** 上面每一个框都应该可以点击

## 行为（市场视图四卡）

| 指标 | 点击 |
|---|---|
| 高优先级 | 筛 `NEW` + `minPriority≥4`，打开「信号明细」 |
| 持仓风险 | 切组合视图，筛持仓 + `RISK`，打开「风险」Tab |
| 短线机会 | 筛 `short_term` + `OPPORTUNITY`，打开「策略」Tab |
| 复盘积压 | 打开待复盘抽屉（已有） |

组合视图四卡：持仓数→持仓 Tab；持仓风险→风险 Tab；证据缺口→右侧持仓风险摘要；组合未处理→筛组合未读 + 信号明细。

`CommandMetric` 的 aria/title/hover 按目标文案，不再写死「打开历史复盘」。
