# MarketOverview 组件说明

## 模块功能

`MarketOverview` 是“大盘云图”一级模块的容器, 承载行业云图、市场共振和板块资金流向。FR-224 后, 顶部标题栏已经常驻展示三大指数摘要, 大盘云图内部不再额外渲染市场环境悬浮球。

FR-243 后，板块资金页优先读取东方财富概念/行业真实主力资金并保存每日观察；外部失败时先回退最近真实存档，再降级为本地“成交方向强度”。降级口径不得称为净流入，也不得生成资金决策信号。
FR-274（本仓合入上游 d562051；避免与 Settings 数据目录 FR-265 撞号）起，板块资金支持按已存档交易日回看：日期导航、历史模式停止 60 秒轮询、本地完整观察优先；缺失存档时稳定失败，不把当日实时接口冒充历史。Migration 157 起废弃无人使用的 `sector_flow_daily` 表与仓库，唯一存档真相为 `sector_flow_observations`。

## 实现思路

组件维护 `activeSubTab` 并写入 `localStorage.marketOverviewSubTab`, 用于恢复用户上次查看的子页面。FR-244 起，二级入口只保留 App 壳层左侧 Flyout，`MarketOverview` 不再渲染重复顶部 Tab；受控 props 和本地存储键保持兼容。

## 主要 props/state/事件流

- `activeSubTab`: 当前子页签, 可为 `industry`、`heatmap` 或 `sectorFlow`; 作为 prop 传入时由外部控制。
- `onSubTabChange`: 历史兼容回调；FR-244 页面本身不再触发，切换统一由 App 壳层 Flyout 完成。
- `IndustryHeatmap`：行业云图。
- `MarketHeatmapPanel`：市场共振工作台，比较上证、沪深300、创业板与31个申万一级行业的一分钟收益关系。
- `SectorFlow`：板块真实资金与次日竞价观察。

## 市场共振

- 主进程 `marketResonanceService` 从东方财富实时/延迟节点读取指数和行业一分钟趋势，并与行业广度、主力净额合并。
- `marketResonanceModel` 计算一分钟收益相关性、同向率、近30分钟持续性、超额收益和±5分钟移位关系；少于30个对齐分钟不输出判断。
- 页面顺序为盘面结论、紧凑市场背景、行业排序和单行业分时下钻；旧涨跌分布大图、涨跌停大图和概念标签叠图已移除。
- 页面内表格允许局部横向滚动，应用文档不得出现横向溢出；默认1680x960和最大化窗口均保持结论、排序与详情层级。
- FR-261 起支持按交易日回看: 复用产业研究 `ResearchDatePicker` 选择交易日, 并提供前后交易日、回到最新和重新补采; 历史模式停止 60 秒轮询, 本地完整快照优先, 缺失时才受控补采。
- 历史响应中 `conceptHeat` 固定为空; 涨跌停时间线优先读 `market_timeline_daily`, 缺失才用同日 `limit_list_daily` 近似。
- 历史补采：探针失败不得短路其余基准/行业请求；无足够真实分钟曲线时抛 `MARKET_RESONANCE_INSUFFICIENT`，禁止 `change:0`/`points:[]` 占位落库或伪装成功。缺基准时 UI 禁用对应 Tab，不以假 0% 填齐。
- 今日（含盘中轮询传入当日 `tradeDate`）一律走单日实时链路（优先 `push2`），不得退化到 `push2his`/`ndays=5`。
- FR-262 起一级行业排序增加确定性的“内部结构”摘要，区分普遍走强、集中领涨、内部分化、普遍走弱和证据不足；摘要来自同日申万二级行业事实，不调用模型，也不生成投资信号。
- 一级行业默认展示全部响应结果，标题明确披露“当前展示N/总数”；状态筛选显示实时命中数，“共振/同步”同时覆盖共振领涨和指数同步，不再用“共振走强”误导方向。
- 二级行业仅在用户展开某个一级行业时按 `tradeDate + parentIndustryCode` 读取，同一时间只展开一组；不提供128个二级行业的全局榜单，也不在后台执行128路分钟轮询。
- 展开表按相对父行业超额排序；选择二级行业后展示二级行业、父级行业和市场基准三条同日曲线。历史分钟无法恢复时继续展示同日日级涨跌、广度和资金，并提供当前组重试，不跨交易日补值；历史日缺少本地板块事实时显式报错，不返回空二级。
- FR-263 新浪行业云图：主快照只拉行业总表；成分股按需请求。列表与成分通道限流冷却相互隔离，成分股 403/429 不冻结主列表。

## 特殊逻辑备忘

- 大盘云图用大色块表达行业情况, 市场环境不能以横条、卡片、面板或悬浮球常驻挤占显示空间。
- 页面级提醒和高优先级未读不在大盘云图内重复展示, 统一通过消息中心查看和跳转。
- 新增或调整子页面时，需要同步更新 `MarketOverviewSubTab` 联合类型、`MARKET_OVERVIEW_SUB_TABS` 白名单和 App 壳层二级 Flyout 配置；不得重新添加页面内同级导航。
- 一级行业名称继续负责打开一级详情，独立展开按钮只负责显示二级结构；展开按钮必须保留至少44px命中区域、`aria-expanded` 和 `aria-controls`，不得把整行改成含混的双重点击入口。
- 板块资金首屏先展示来源、明早竞价结论、去重重点主题、风险主题和核心股，完整明细位于其后；禁止恢复跨重叠板块的“全市场净流入合计”。
- 成分抓取候选与结论必须使用同一评分排序；成分缺口降低置信度并显式说明，缺失比率保持 `null/--`。
